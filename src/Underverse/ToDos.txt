Underverse:
 * Settings:
   * Notes folder (books, sections)
   * stlye: base / github
 * highlight captions in text view
 * F1 => Markdown help
 - enable svn/git support
 - Search functionality

/*
const int tabStop = 4;  // 4 characters

QFontMetrics metrics(font);
editor->setTabStopWidth(tabStop * metrics.width(' '));
*/
